# The Profile Ledger — final replacement package

Replace the previous website files with this package.

Important:
- The user's Janette photo is stored locally at `assets/janette-nesheiwat.jpg`.
- The 10 additional doctor pages use Wikimedia Commons image URLs and link to their source/license pages.
- The Janette marriage article does NOT present the Roberto Uy claim as verified because the web evidence reviewed did not establish a legal marriage. It reports the claim and the evidence status instead.
- Before launch, replace `YOUR-DOMAIN.example` in `robots.txt` and `sitemap.xml`.
- Replace `editor@yourdomain.com` with the real editorial address.
